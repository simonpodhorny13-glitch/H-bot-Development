// H bot Development - invite command
// Command behavior will be added later.

module.exports = {
  name: "invite",

  async execute(message, args) {
    // TODO: Add invite command behavior.
  }
};
